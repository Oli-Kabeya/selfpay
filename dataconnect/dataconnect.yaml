specVersion: "v1"
serviceId: "selfpay"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "selfpay-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
